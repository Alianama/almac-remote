name: AlmacRemote
options:
  bundleIdPrefix: id.my.alipurnama
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  MRNG:
    path: .
  SwiftTerm:
    url: https://github.com/migueldeicaza/SwiftTerm
    from: "1.2.0"

targets:
  AlmacRemote:
    type: application
    platform: macOS
    sources:
      - App
    dependencies:
      - package: MRNG
        product: MRNGCore
      - package: SwiftTerm
        product: SwiftTerm
    info:
      path: App/Info.plist
      properties:
        CFBundleDevelopmentRegion: en
        CFBundleLocalizations: [en, ro, pl]
        # Version + build come from MARKETING_VERSION / CURRENT_PROJECT_VERSION
        # in `settings.base` below — single source of truth, bump there only.
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSHumanReadableCopyright: "Almac Remote — based on mRemoteNXT, Copyright © 2026 Razvan Cremenescu. Released under GPL-2.0-or-later."
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
        LSApplicationCategoryType: public.app-category.utilities
        # macOS 15+/Tahoe gate connections to LAN hosts behind the Local Network
        # privacy permission; without this key the TCC prompt never appears and
        # RDP/SSH to local servers fails at the TCP transport
        # (ERRCONNECT_CONNECT_TRANSPORT_FAILED).
        NSLocalNetworkUsageDescription: "Almac Remote connects to servers on your local network for RDP, SSH, Telnet, SFTP and HTTP sessions."
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: id.my.alipurnama.AlmacRemote
        PRODUCT_NAME: Almac Remote
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        MARKETING_VERSION: "0.8.1"
        CURRENT_PROJECT_VERSION: "16"
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGNING_ALLOWED: YES
        ENABLE_HARDENED_RUNTIME: YES
        SWIFT_VERSION: "5.9"
        SWIFT_OBJC_BRIDGING_HEADER: App/Bridging-Header.h
        CLANG_ENABLE_MODULES: YES
        HEADER_SEARCH_PATHS:
          - /opt/homebrew/opt/freerdp/include/freerdp3
          - /opt/homebrew/opt/freerdp/include/winpr3
          - /opt/homebrew/opt/openssl@3/include
        LIBRARY_SEARCH_PATHS:
          - /opt/homebrew/opt/freerdp/lib
          - /opt/homebrew/opt/openssl@3/lib
        OTHER_LDFLAGS:
          - -lfreerdp-client3
          - -lfreerdp3
          - -lwinpr3
          # OSSL_PROVIDER_* (legacy provider load for NTLM/MD4) is called
          # directly from RDPCore.c, so link libcrypto explicitly rather than
          # relying on the transitive dep through FreeRDP.
          - -lcrypto
